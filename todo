* vbscript|node|deno|bun: se puede usar el comando en batch cscript, para ejecutar scripts .vbs o scripts de vbscript, esto puede ayudar a pasar los comandos de vbscript a python, al crear script donde ejecute un solo comando y devuelva su errorcode.
